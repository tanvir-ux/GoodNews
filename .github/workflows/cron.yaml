name: 15-minute-cron
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:      
      - name: cron job
        shell: bash
        run: bash timeout.sh
          
          